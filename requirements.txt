# OWID Data Tools — dependencies
#
# Install with:
#   pip install -r requirements.txt

# Core dependencies (also needed by owid_tools.py)
owid-catalog>=0.2.0
pandas>=1.5.0
pydantic>=2.0.0

# MCP server framework (Streamable HTTP + OAuth 2.1 support)
fastmcp>=3.4.0
